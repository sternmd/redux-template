import React from 'react';

import HomeContainer from '../containers/arist_container';

const Artist = (props) => {
    return (
        <div>
            <ArtistContainer {...props} />
        </div>
    )
}

export default Artist;
